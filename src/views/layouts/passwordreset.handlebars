<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.login.css">
    
<h1>Correo de restablecimiento de contraseña</h1>

<div class="reset-password-container row justify-content-center align-items-center" >
    <h2>Restablecer Contraseña</h2>
    <form action="/api/users/requestPasswordReset" method="POST">
        <div class="form-group">
            <label for="email">Correo Electrónico:</label>
            <input type="email" id="email" name="email" required>
        </div>
        <button type="submit" class="btn-submit">Enviar enlace de restablecimiento</button>
    </form>

    {{#if error}}
        <p class="error-message">{{error}}</p>
    {{/if}}